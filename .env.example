# Environment Variables Template
# Copy this file to .env for local development (do not commit .env)

# Hugo Configuration
HUGO_ENVIRONMENT=development
# HUGO_ENVIRONMENT=production  # Uncomment for production builds

# Site Configuration
# BASEURL=https://nclsbayona.github.io  # Override base URL if needed

# Build Options
# HUGO_BUILD_DRAFTS=true   # Include draft posts in build
# HUGO_BUILD_FUTURE=true   # Include future-dated posts

# GitHub Actions (automatically set in CI)
# GITHUB_TOKEN=<automatically provided by GitHub Actions>
# GH_TOKEN=<for semantic-release>

# Quote Script Configuration (if needed)
# QUOTE_API_URL=<quote API endpoint>
# QUOTE_API_KEY=<API key if required>

# Development Tools
# HUGO_CACHEDIR=./hugo_cache  # Custom cache directory
# HUGO_RESOURCEDIR=./resources  # Custom resource directory

# Performance Monitoring
# LIGHTHOUSE_URL=http://localhost:1313  # URL for Lighthouse testing

# Security Testing
# ZAP_TARGET_URL=https://nclsbayona.github.io  # Target for ZAP scans

# Notes:
# - Do not commit sensitive information to version control
# - Use GitHub Secrets for CI/CD environment variables
# - Most Hugo config can be set in config/_default/ instead
